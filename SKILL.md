---
name: grounded-roundtable
description: Use when turning two or more YouTube interviews, talks, or presentations into a clearly virtual, source-grounded long-form roundtable and Google Slides deck with natural Korean dialogue, native YouTube evidence players, and timestamped fallback links.
---

# Grounded Roundtable

## Core contract

Create a thinking aid, not a fake historical exchange. The result combines separate videos into a virtual conversation whose claims can be checked against the original footage. Keep the virtual label visible on every slide and never imply that the participants heard or answered one another.

## Workflow

1. **Collect the input contract.** Require at least two YouTube URLs, speaker/video metadata, one topic (or propose the strongest common topic), a lens, a dialogue direction, and the intended audience. Use `same-domain` unless the user wants to connect different fields; then use `cross-domain`. Keep the lens separate from direction. Offer `tension-first`, `common-ground-first`, `forecast-to-choice`, and `solutions-first`; do not let the topic silently determine either input. Ask for one desired takeaway or propose a single sentence after comparing the sources; use it as the final message slide, not as a license to overstate the sources.
2. **Ingest once, then cache.** Retrieve metadata and captions/transcripts. Retry after the page/player finishes loading before declaring captions unavailable. If captions remain unavailable, request a transcript or produce an explicitly incomplete draft. Do not invent timestamps.
3. **Build the evidence map first.** For every participant claim store video ID, start/end seconds, timestamp URL, transcript span, source status, and a confidence note. Validate each anchor before composing dialogue. Before rendering, add an `evidence_uses` render plan and run `scripts/validate_manifest.mjs`; it rejects repeated anchor IDs, repeated exact clips, slide IDs with more than one player, and range/video mismatches.
4. **Compose long-form scenes.** Use 3–4 scenes with 6–10 turns each and 2–4 evidence anchors after a meaningful stretch of conversation. Let the moderator create continuity; participant turns are `direct quote`, `faithful paraphrase`, or `multi-anchor synthesis`. A participant turn contains one atomic claim. For `cross-domain`, read [cross-domain bridges](references/cross-domain-bridges.md): the moderator may propose an AI-labeled connection only after both source positions are shown, and must pair the shared principle with a consequential practice difference.
5. **Edit for Korean speech.** Treat transcript translation as raw material, not final copy. Rewrite into natural spoken Korean with clear subjects, ordinary connective endings, and short clauses; remove English word order, noun-stack chains, and repeated “~하는 것이” constructions. When a source uses a metaphor, convert it through `source image → underlying claim → shared decision question`: keep the image on the evidence or visual layer, and write the dialogue at the claim level. Keep the source's strength, uncertainty, and conditions. Read [Korean naturalness pass](references/korean-naturalness.md), then do a final read-aloud pass while checking every sentence against its anchor.
6. **Render the deck.** Prefer native Google Slides when connected; otherwise produce a PPTX that imports cleanly. Use scene-centered conversation pages followed by evidence-anchor pages. If the user asks for a speaker-first flow, use `evidence-focus`: one concise speaker-claim slide followed by one full-size evidence slide with one native player. For native Slides, insert YouTube videos as `YOUTUBE` video elements using the original Video ID, set `videoProperties.start`/`end` to the evidence range, and set `autoPlay=false` unless the user requests autoplay. Retain a timestamp link beside the player as an external/open-in-YouTube fallback. Keep actual source players visually distinct from synthesized dialogue and include the short disclosure plus source notes. Use a read–modify–read transaction: update an existing player in place, or delete the exact fallback image before creating one; never create a second player in the same media slot. After the batch update, verify exactly one native player and zero images per evidence slide with `scripts/validate_native_structure.mjs` or an equivalent readback.
7. **Use a high-contrast visual system.** Default to a charcoal/navy canvas (`#0E1726` or similar), white body copy, and yellow (`#FFD34E`) for titles, key phrases, rules, and links. Never use yellow as a full-slide or large panel background. Keep titles large enough to scan at presentation distance (about 40–54 pt in a 16:9 deck), body copy concise (about 20–30 pt), and leave generous negative space. Do not add fake player frames, black overlays, or decorative play buttons when a native video element is present.
8. **Show provenance on the evidence slide.** Place a compact, readable source block beside the player with the YouTube channel name (linked to the channel), the original video link, speaker, Video ID, exact clip range, and caption/source status. Prefer the channel name and channel URL as the human-facing citation; keep the video URL as the direct evidence link. If channel/title metadata is not verified, write `제공된 원본 링크` rather than guessing. The visible source block and the `[Sources]` notes block must agree.
9. **Verify before delivery.** Check timestamp URLs, speaker/anchor mapping, caption status, evidence-anchor uniqueness, slide overflow, link preservation after import, native video element IDs/Video IDs, the visible source block, and the disclosure. If tokens or time are tight, reduce topic count, scenes, turns, and decoration in that order; never skip source verification. A dialogue continuation may mention an earlier idea in prose, but it must not repeat the earlier evidence card or exact clip.

Read the focused references only when needed: [dialogue rules](references/dialogue-rules.md), [Korean naturalness pass](references/korean-naturalness.md), [source contract](references/source-contract.md), [Slides structure](references/slides-structure.md), [cost budget](references/cost-budget.md), and [cross-domain bridges](references/cross-domain-bridges.md) only for the `cross-domain` lens.

## Red flags

Stop and revise if a synthetic line is in quotation marks, says “you are right” or “your point,” assigns an AI bridge to a participant, states a cross-domain commonality without a visible difference, compares unmatched source metaphors instead of their underlying claims, uses an unverified timestamp, treats automatic captions as final verbatim text, places a clip after every sentence, repeats an anchor/player without an explicit reuse reason, leaves a thumbnail under a native player, or hides the virtual disclosure in a single appendix slide.

